import { Base, BaseType } from "./models";

// ===================== world settings =====================

export const playerSide: BaseType = BaseType.Player2;

// ===================== world objects =====================

export const memory: object = {};

// export const base = playerSide === BaseType.Player1 ?
//     new Base(BaseType.Player1) :
//     new Base(BaseType.Player2);

// export const enemy_base = playerSide === BaseType.Player1 ?
//     new Base(BaseType.Player1) :
//     new Base(BaseType.Player2);
